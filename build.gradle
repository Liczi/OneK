plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.3.72' apply false
}

allprojects {
    group 'licznerski.jakub'
    version '1.0-SNAPSHOT'
}

subprojects {
    repositories {
        mavenCentral()
    }
}