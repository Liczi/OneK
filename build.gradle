plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.4.0' apply false
}

allprojects {
    group 'licznerski.jakub'
    version '1.0-SNAPSHOT'
}

subprojects {
    repositories {
        mavenCentral()
        jcenter()
    }
}